{
  "name": "gatsby-starter-default",
  "description": "Gatsby",
  "version": "1.0.1",
  "dependencies": {
    "gatsby": "^1.9.119",
    "gatsby-link": "^1.6.28",
    "gatsby-plugin-react-helmet": "^1.0.8",
    "gatsby-plugin-sitemap": "^1.2.25",
    "gatsby-plugin-styled-components": "^2.0.2",
    "gatsby-source-filesystem": "^1.5.8",
    "gatsby-transformer-remark": "^1.7.21",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-ga": "^2.3.5",
    "styled-components": "2.2.0"
  },
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "start": "gatsby develop --host localhost --port 8000",
    "build": "gatsby build",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write \"src/**/*.js\"",
    "deploy": "yarn build && aws s3 sync public s3://holdonnn.me --delete  --region ap-northeast-1  --acl public-read --cache-control 'max-age=31536000, public'",
    "cache-bust": "aws cloudfront create-invalidation --distribution-id E35V23RZLIKY75 --paths '/*'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-serve": "yarn build && serve public"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "prettier": "^1.8.2"
  }
}
